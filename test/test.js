import { expect } from "chai";

describe(`FxDOM`, function () {
  it(`1 + 1 = 2`, function () {
    expect(1 + 1).equal(2);
  });

  it(`true === false`, function () {
    expect(true).false;
  });
});
